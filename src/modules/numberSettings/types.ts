export type NumberSettingsState = {
  minNum: number;
  maxNum: number;
  count: number;
  delay: number;
  autoGenerate: boolean;
  uniqueOnly: boolean;
  pressToStart: boolean;
  shakeToStart: boolean;
};
